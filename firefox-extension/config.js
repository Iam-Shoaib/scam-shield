// Shared config, loaded before background.js (classic script, shared global scope).
//
// For local testing against `npm run dev`, comment out the line below and
// uncomment the localhost one instead. Whichever origin you use here must
// also be listed in manifest.json's host_permissions.

const API_ORIGIN = "https://scam-shield-rouge.vercel.app";
const WEB_APP_ORIGIN = "https://scam-shield-rouge.vercel.app";

// const API_ORIGIN = "http://localhost:3000";
// const WEB_APP_ORIGIN = "http://localhost:3000";
